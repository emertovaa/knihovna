<form method="post" action="{{ path('app_autor_delete', {'id': autor.id}) }}" onsubmit="return confirm('Opravdu chcete odstranit tuto položku?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autor.id) }}">
    <button class="btn btn-danger">Smazat</button>
</form>
