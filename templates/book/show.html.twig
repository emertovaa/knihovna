{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
    <h1>Book</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book.id }}</td>
            </tr>
            <tr>
                <th>Kniha</th>
                <td>{{ book.kniha }}</td>
            </tr>
            <tr>
                <th>Autor</th>
                <td>{{ book.autor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_book_index') }}" class="btn btn-warning">Zpět na hlavní stránku</a>

    <a href="{{ path('app_book_edit', {'id': book.id}) }}" class= "btn btn-info">Upravit</a>

    {{ include('book/_delete_form.html.twig') }}
{% endblock %}
